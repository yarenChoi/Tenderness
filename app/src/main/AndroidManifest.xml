<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yarenchoi.tenderness">

    <!-- GalleryFinal -->
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- 更改语言环境 -->
    <uses-permission android:name="android.permission.CHANGE_CONFIGURATION" />
    <!-- 录音权限 -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <application
        android:name=".BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activity.MemoryEditActivity" />
        <activity android:name=".ui.activity.RecordingActivity" />
        <activity android:name=".ui.activity.MemoryShowActivity" />
        <activity android:name=".ui.activity.AuthorActivity" />
        <activity android:name=".ui.activity.CodeBookActivity" />
        <activity android:name=".ui.activity.SettingsActivity" />
        <activity
            android:name=".ui.activity.PlayVoiceActivity"
            android:theme="@style/DialogActivityStyle" />
        <activity android:name=".ui.activity.PhotoPreviewActivity" />
    </application>

</manifest>